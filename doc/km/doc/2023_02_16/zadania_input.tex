\noindent
\hspace{7cm}
\textbf{ZADANIA 16/02/2023}\\

\noindent
\textbf{Zadanie 1} W trójk¹cie $ABC$ punkty $A'$, $B'$ i $C'$ le¿¹ odpowiednio na bokach $BC$, $CA$ i $AB$. Proste $AA'$, $BB'$, $CC'$ przecinaj¹ siê w punkcie $O$. Wyznacz 
$\frac{AO}{OA'} \cdot \frac{BO}{OB'} \cdot \frac{CO}{OC'}$, jeœli wiadomo, ¿e $\frac{AO}{OA'} + \frac{BO}{OB'} + \frac{CO}{OC'} = 92$.\\

\noindent
\textbf{Zadanie 2} Niech $P_2(x) = x^2 - 2$. Wyznacz wszystkie ci¹gi wielomianów $\{P_k(x)\}_{k=1}^{\infty}$, ¿e $P_k(x)$ jest stopnia $k$, wspó³czynnik przy $x^k$ w wielomianie $P_k(x)$ wynosi jeden 
oraz $P_i(P_j(x)) = P_J(P_i(x))$ dla dowolnych $i,j=1,2,\ldots$.\\

\noindent
\textbf{Zadanie 3} Niech $\frac 34 < a < 1$. Rozwi¹¿ równanie: $x^3(x+1) = (x + a) (2x + a)$. \\

\noindent
\textbf{Zadanie 4} Udowodnij, ¿e istnieje nieograniczony, rosn¹cy ci¹g $\{a_n\}$, $n = 1, 2, \ldots$ dodatnich liczb ca³kowitych, taki, ¿e istnieje dodatnia liczba ca³kowita $M$ o w³asnoœci: dla ka¿dej liczby ca³kowitej $n\geq M$ wszystkie dzielniki pierwsze $n! + 1$ s¹ wiêksze od $n + a_n$, przy za³o¿eniu, ¿e $n + 1$ nie jest liczb¹ pierwsz¹.\\

\noindent
\textbf{(02/02/2023) Zadanie 1}\\
\textbf{nierównoœæ Cauchy'ego--Schwarza}: Dla liczb rzeczywistych $a_1, a_2, \ldots, a_n$, $b_1, b_2, \ldots, b_n$ zachodzi nierównoœæ:
$$
(a_{1}^{2} + a_{2}^{2} + \ldots + a_{n}^{2})(b_{1}^{2} + b_{2}^{2} + \ldots + b_{n}^{2}) \geq (a_1 b_1 + a_2 b_2 + \ldots + a_n b_n)^2
$$
\textbf{nierównoœæ Jensena}: dla dowolnej funkcji wypuk³ej $y = f(x)$ w przedziale $I$ oraz $x_1, x_2, \ldots, x_n \in I$, nieujemnych liczb rzeczywistych $t_1, t_2, \ldots, t_n$, takich, ¿e $t_1 + t_2 + \ldots + t_n = 1$ zachodzi nierównoœæ:
$$
t_1 f(x_1) + t_2 f(x_2) + \ldots + t_n f(x_n) \geq f(t_1 x_1 + t_2 x_2 + \ldots + t_n x_n)
$$
Wykorzystuj¹c powy¿sze nierównoœci (obie lub jedn¹ z nich) udowodnij, ¿e zachodz¹ nierównoœci:\\
(a) $a,b,c,d > 0$, wtedy:
$$
\frac{1}{a + b + c} + \frac{1}{a + b + d} + \frac{1}{a + c + d} + \frac{1}{b + c + d} \geq \frac{16}{3(a + b + c + d)}
$$
(b) $a,b,c>0$, wtedy:
$$
\frac{a}{\sqrt{a+b}} + \frac{b}{\sqrt{b+c}} + \frac{c}{\sqrt{c+a}} < \sqrt{2(a+b+c)}
$$
(c) $a, b, c > 0$, wtedy:
$$
\frac{a}{b + c} + \frac{b}{c + a} + \frac{c}{a + b} \geq \frac 32
$$
(d) $a_1, a_2, \ldots a_n > 0$, wtedy:
$$
\frac{a_1 + a_2 + \ldots + a_n}{n} \geq \sqrt[n]{a_1 a_2 \ldots a_n}
$$
(e) $a, b, c > 0$, wtedy:
$$
\sqrt{ab^2(b+c)} + \sqrt{bc^2(c+a)} + \sqrt{ca^2(a+b)} \leq \frac{1}{2} \sqrt{(a+b+c)^4 - (a^2 + b^2 + c^2)^2} 
$$


\noindent
\textbf{(26/01/2023) Zadanie 1} 
Wykazaæ, ¿e dla ka¿dej liczby pierwszej $p > 3$ istniej¹ liczby ca³kowite $x$, $y$, $k$ spe³niaj¹ce warunki $0<2k<p$ oraz 
$$
kp + 3 = x^2 + y^2.
$$

\noindent
\textbf{(05/01/2023) Zadanie 1} Uzasadnij, ¿e kwadratu nie sie podzieliæ na piêæ kwadratów.\\

\noindent
\textbf{(05/01/2023) Zadanie 2} \\
Udowodnij nastêpuj¹ce twierdzenie (\textbf{Lemat Halla}): Niech na pewnym przyjêciu znajduje siê $n$ ch³opców i $n$ dziewcz¹t, ka¿dy ch³opiec zna pewn¹ grupê dziewcz¹t. Udowodniæ, ¿e jeœli wœród dowolnego zbioru $k$--elementowego ch³opców ($k=1,2,\ldots,n$) iloœæ dziewcz¹t znanych przez tych ch³opców jest nie mniejsza od $k$, to da siê tak dobraæ ch³opców i dziewczêta w pary aby w ka¿dej parze by³y osoby, które siê znaj¹.\\